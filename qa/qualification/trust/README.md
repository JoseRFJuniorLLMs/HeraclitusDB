# Qualification trust roots

Place public verification material here through the controlled release
process. `lab.pub` is deliberately not committed: a missing trust root makes
external attestations `Inconclusive` and cannot be bypassed by the qualifier.

Private keys and HSM credentials must never enter this repository or an
evidence bundle.
